# 2026-07-23 工作日志

## git 推送
- 提交 `7bc676b`：fix(launcher/splash) 启动屏拆独立进程+进度条出现才淡出；部署维护脚本检查一致性；UM 登录门控；docs 更新。（147 文件，含 launcher/main/build-version/yunji_splash/ace-step-ui/verify_exe 等）
- 推送 origin(github: yunjii-cn/music) + gitee(镜像) **均成功**：`543d340..7bc676b main -> main`。
- 注：`git add -A` 把 `dev/app/build_tools/*`（7z/upx 等大二进制）也提交了——这些本应 gitignore，下次清理 .gitignore 或 `git rm --cached`。

## 一键部署维护流程分析（结论：不完善，≥5 处缺陷）
代码位置：`dev/app/main.py` 的 `_deploy_maintenance_thread`(5418) + `_ensure_scripts_available`(7693 区) + `install-env.ps1` + `find_venv_python`(517)。

1. **[致命] venv 路径不一致**——`install-env.ps1:76` 把 venv 建在 `$PSScriptRoot\..\..\data\.venv`（即 `项目根/data/.venv`），而 `main.py` 全程在 `base_dir/scripts/.venv`（即 `app/scripts/.venv`）查找（5653/2964/3330/5199 + `find_venv_python`:517/538 返回 `scripts/.venv/Scripts/python.exe`）。→ `environment_installed = bool(uv存在 and venv文件夹存在)` 中 venv 永远找不到 → **每次都判"未完全安装"并重跑安装**，状态页永远显示 venv 缺失。这是让整个流程"跑不完"的根因。

2. **[体验] "卡住"是假象**——`install-env.ps1` 用 `-WindowStyle Hidden` 隐藏窗口 + `hidden_run(..., capture_output=False)`（main.py:5692）+ `subprocess.run` **阻塞部署线程**（timeout=1800）。UI 全程只显示"4. 执行环境安装脚本..."，无任何进度流。实际在下载 `torch==2.9.0 torchaudio==2.9.0`（cu128，约 2-3GB）+ transformers/diffusers/gradio 等，**确实要十几分钟**。用户无法区分"在跑"还是"卡死"。

3. **[不一致] 解包列表 ≠ 检查/启动列表**——`scripts_to_extract`(7693) 只解 `install-env.ps1`+`start.ps1`，但环境检查(4701) 与"启动 API 服务"(3045) 都硬编码引用 `3、run_server.ps1` → 全新部署永远报"⚠️ 脚本不存在: 3、run_server.ps1"，且启动 API 时报"脚本不存在"错误。

4. **[脆弱] 脚本文件名带中文编号前缀**——`1、install-uv-qinglong.ps1` / `3、run_server.ps1` 等非 ASCII 文件名，跨部署易丢失（用户 `D:\test\...` 部署即缺 `3、run_server.ps1`）。且 main.py 多处硬编码引用，改名牵一发动全身。

5. **[健壮性] 安装完成判定过弱 + 无取消**——`environment_installed` 仅判 uv+venv 文件夹存在；半残 venv（安装被 30min 超时杀掉）仍判 True → 跳过重装 → 坏环境持续。整个部署线程无 stop 标志/取消按钮，真卡死只能等超时。

### 建议修复方向（待用户确认是否动手）
- A. venv 路径对齐：改 `install-env.ps1:76` 为 `$venv_dir = Join-Path $PSScriptRoot ".venv"`（与 main.py 一致），或反之统一到 `data/.venv`。
- B. 安装可见性：把 5692 的 `hidden_run(capture_output=False)` 改为 `subprocess.Popen(stdout=PIPE,stderr=PIPE)` 循环读行 → `self._log()` 实时喂 UI；至少去掉 `-WindowStyle Hidden`。
- C. 解包/检查/启动三处脚本列表对齐，并去掉中文前缀（改名 run_server.ps1 等 + 全量替换 main.py 引用）。
- D. `environment_installed` 增加 torch 可导入校验；加"取消"按钮 + 看门狗。

## 一键部署维护流程 A→C→B→D 四项修复（已实施，用户"按推荐方式进行"授权）
- **A venv 路径对齐**：`install-env.ps1` 把 venv 从 `..\..\data\.venv` 改为 `$PSScriptRoot\.venv`（与 main.py 的 `find_venv_python`/各处检查一致）。删掉多余的 `$data_dir` + `New-Item data_dir`，消息文本 `(data\.venv)`→`(scripts\.venv)`。加注释警告"切勿改回 data\.venv"。这是让部署"永远跑不完/重复装十几分钟"的**根因修复**。
- **C 脚本改名+列表对齐**：用 `git mv` 去掉 6 个脚本的中文编号前缀（`1、install-uv-qinglong.ps1`→`install-uv-qinglong.ps1` 等，保留 git 历史）。main.py 全仓 9 处中文编号引用 `replace_all` 替换为干净名（PROJECTS 字典、scripts_to_check、scripts_to_extract、api_script、install_script、while os.path.exists 等）。`scripts_to_check` 扩为 8 个运行期脚本；`_MEI` 释放列表 `scripts_to_extract` 扩为 10 个（含 start-all.ps1、install-uv-qinglong.ps1、acestep-sync.ps1）。`start-all.ps1` 内 `3、run_server.ps1` 引用同步更新。
- **B 安装进度可见**：新增 `_stream_powershell(script_path, cwd, timeout=1800)`——用 `powershell -Command` 强制 UTF-8 + `& '脚本'` 流式跑，`hidden_popen(stdout=PIPE,stderr=STDOUT,text=True,encoding=utf-8,bufsize=1)` + `readline()` 逐行按关键字染色喂 `_log`；支持取消(-2)/超时(-1)/启动失败(-3)/正常 returncode。原 5712 附近阻塞 `hidden_run(-WindowStyle Hidden, capture_output=False)` 替换为 `rc = self._stream_powershell(install_script, ...)` + 按 rc 分支日志化。
- **D 判定增强+取消**：环境判定从"仅判 venv 文件夹存在"升级为 `venv_complete`（含 `Scripts/python.exe` + `Lib/site-packages/torch`），半残 venv 不再误判通过、会提示"虚拟环境不完整，将重新安装"。UI 新增"✖ 取消"按钮 `btn_cancel_deploy`（初始隐藏，部署中显示）；新增 `_cancel_deploy()`（置 `_deploy_cancel=True` + `proc.terminate()`）；`_deploy_maintenance()` 开头复位标志并显示取消键；`_enable_buttons()` 末尾隐藏取消键。
- **校验**：main.py `ast.parse` 通过；`install-env.ps1`+`start-all.ps1` PowerShell Parser 校验通过；全仓 grep `[1-6]、` 已无脚本引用残留。
- **build 确认**：`build_light.py`→`build_exe()` 的 `scripts_dir.glob("*.ps1")` 会自动打包所有改名脚本，`--add-data main.py` 已存在——改动随发布自动生效，**无需改 build 脚本**。

## 一键部署维护实测验证（用户 04:44-04:56 实测日志）
- ✅ A-D 四项修复全部验证通过：venv 正确创建在 `scripts/.venv`、所有脚本均找到、流式进度实时显示（torch 2.7GiB 下载可见）、环境判定正确
- ⚠ 发现的问题：transformers 5.14.1 被安装后又被「智能环境修复」降级（兼容性问题）；"智能环境修复"与 install-env.ps1 的工作部分重叠，冗余检查约 69 秒；主流程步骤编号跳到 7（无步骤 6）

## UI 改动：运行日志按钮调整（07-23 05:00 用户要求）
- `🔄 自动滚动` → `🔄 滚动`（两处：主日志 + 部署维护日志）
- 新增 `📋 复制` 按钮（两处：`_copy_runtime_log` + `_copy_deploy_log`，使用 `QApplication.clipboard()` 复制日志到剪贴板）
- 对应注释/文档/帮助文字同步更新
- 已通过 `ast.parse` 校验，轻量构建新 exe `v2026.07.23.0520`

## 三项部署流程优化（用户实测后授权"按推荐方式进行"）
### ① transformers 版本锁定
- `install-env.ps1` 第164行：transformers 从无版本约束改为 `"transformers>=4.51.0,<4.58.0"`，避免安装最新 5.x 后被 main.py 降级（装→降浪费 ~2 分钟）
- 新增警示输出 `⚠ 锁定 transformers<4.58.0 避免版本不兼容降级`

### ② install-env 成功后复用标志，跳过冗余复检
- `_deploy_maintenance_thread` 步骤4（install-env.ps1）成功后，将 `environment_installed` 置为 True
- 使步骤5走 `_quick_verify_environment()`（仅检查不做安装）而非 `_smart_fix_environment()`（完整检查+安装），跳过约 69 秒的重复依赖验证

### ③ 智能修复内部步骤编号优化
- `_smart_fix_environment()` 内部 12 个步骤全部增加缩进前缀（如 `"  1. 检查 PowerShell..."`），与外部步骤 1-7 视觉上明确区分
- 修复重复编号 bug：原"11. 检查配置文件" + "11. 最终环境检查" → 改为"11. 检查配置文件" + "12. 最终环境检查"

## run_*.ps1 全部脚本 venv 路径修复（07-23 06:20，用户测得「一键部署完成后无法启动」）
- **根因**：run_server.ps1 / run_gradio.ps1 / run_npmgui.ps1 / run_qinglong_backend.ps1 / start.ps1 **全部硬编码了旧路径 `data/.venv`**，而实际 venv 在 `scripts/.venv`
- **修复**：全部 5 个脚本的 venv 路径从 `..\data\.venv` / `..\..\data\.venv` 统一改为 `$PSScriptRoot/.venv` 或 `$ScriptDir/.venv`
- **共改文件**：`run_server.ps1`、`run_gradio.ps1`、`run_npmgui.ps1`（2处）、`run_qinglong_backend.ps1`、`start.ps1`

## flash_attn wheel 构建打包 + 下载回退
- **问题**：`build-version.py` �� glob `*.ps1`，wheel 文件未被加入 `--add-data`；运行时 wheel 文件不存在于部署目录
- **修复**：`build-version.py` 增加 `scripts_dir.glob("flash_attn*.whl")` 加进 `--add-data`；`main.py` 的 `scripts_to_extract` 增加该 wheel 文件名
- `install-env.ps1` 增加下载回退：本地 wheel 不存在时，从 GitHub Releases 下载（`https://github.com/yunjii-cn/music/releases/download/wheels/flash_attn-*.whl`）
- 需要用户在 GitHub Releases 创建 `wheels` tag 上传该文件

## 初始化后自动部署（新手引导）
- `_on_init_finished` 成功后 → 1 秒后自动调用 `_auto_deploy_after_init()` → 切到部署维护页 → 执行 `_deploy_maintenance()`
- 部署完成（`_enable_buttons`）后自动导航到模型管理页面，提示下载主模型
- 新增 `_auto_deploy_initiated` 标志（初始 False），仅在初始化后的自动部署中置 True，防止手动部署时也跳转

## ⚠️ 修正：flash_attn wheel 不应打包进 exe（用户 07-23 明确纠正）
- 用户原话："我不说了吗？在仓库的下载页上传flash_attn wheel文件，然后获得下载地址，加入一键部署维护，而不是直接打包到exe"
- **已回退**：`build-version.py` 的 `scripts_dir.glob("flash_attn*.whl")` 已删除；`main.py` 的 `scripts_to_extract` 已移除 wheel 文件名
- **最终方案**：wheel 只通过 `install-env.ps1` 的下载回退获取（GitHub Releases `wheels` tag），绝不打进单文件 exe
- 教训：上面"构建打包 + 下载回退"一节中"build-version.py 增加 glob wheel 加进 --add-data"是**错误做法**（导致 `v2026.07.23.0624.exe` 暴涨到 272MB），以本节为准
- 验证：二进制扫描确认 `v2026.07.23.0930.exe` (37MB) 内 wheel 字符串出现 **0** 次；误打包的 `v2026.07.23.0624.exe` (272MB) 出现 1 次
- **用户待办**：在 `https://github.com/yunjii-cn/music/releases` 创建 `wheels` tag 并上传 `flash_attn-2.8.3+cu128torch2.9.0cxx11abiTRUE-cp312-cp312-win_amd64.whl`（文件已在 `dev/app/scripts/` 磁盘上，250MB），下载地址才会生效

## 已交付：v2026.07.23.0930.exe（wheel 纠正版，37MB，已被 1804 取代）
- 路径：`dist/云集智能音乐创意台-v2026.07.23.0930.exe`（同时复制到 `build/`）
- 含：venv 路径统一 `scripts/.venv`、UI 滚动/复制按钮、三项部署优化、自动部署跳转、transformers 锁版本、flash_attn 下载回退；**不含** wheel（37MB vs 误打包 272MB）
- 后被 `v2026.07.23.1804.exe` 取代（加入了 install-uv-qinglong.ps1 的 venv+SkipModels 修正）

## ✅ 已修复并交付：install-uv-qinglong.ps1（用户"继续"授权）
- 该脚本（被 `_check_environment` L3417、`_install_uv` L5261 调用）第81行 venv 改为 `scripts\.venv`；新增 `[switch]$SkipModels` 参数，两处 `Read-Host` 模型选择改为 `if ($SkipModels) { $x="n" } else { Read-Host ... }`，**避免隐藏 PowerShell 下挂起**；4 处 `Write-Output` 的 "data\.venv" 文案同步改为 "scripts\.venv"
- `main.py` 两处调用均追加 `"-SkipModels"` 参数
- PowerShell `Parser::ParseInput` 校验 `ERRORS=0 / SYNTAX_OK`；`main.py` `ast.parse` 通过
- 一并 `git commit`（cf36935）：含 run_*.ps1/start.ps1/start-all.ps1 的 venv 修正、install-env.ps1 锁版本+flash_attn 下载回退、build-version.py/main.py 移除 wheel 打包、install-uv-qinglong.ps1 修正、main.py UI/自动部署等；`.workbuddy` 内存不提交

## 已交付：v2026.07.23.1804.exe（install-uv-qinglong 修正版，37MB）
- 路径：`dist/云集智能音乐创意台-v2026.07.23.1804.exe`（同时复制到 `build/`）
- **二进制提取 CArchive 实测**：内嵌 `install-uv-qinglong.ps1` 含 `param([switch]$SkipModels)`、`$dataVenv = Join-Path $PSScriptRoot ".venv"`、`if ($SkipModels){...}else{Read-Host...}`、且无 `data\.venv` 残留 → 修正确已打进 exe
- 大小 37MB（wheel 仍不打包 ✅）
- 至此**全项目 venv 路径统一为 `scripts/.venv`**，且 install-uv-qinglong 这条独立路径不再创建死路 venv、也不再因 `Read-Host` 挂起
- 该脚本（被 `_check_environment` L3417、`_install_uv` L5255 调用）第81行仍创建 `data\.venv`，与全项目规范的 `scripts\.venv` 不符 → 创建的 venv 无人使用
- **与一键部署无关**：一键部署走 `_deploy_maintenance_thread` → `install-env.ps1`（已正确用 `scripts\.venv`），run_*.ps1 也均已修正为 `scripts\.venv`，闭环可启动
- 该脚本另有**交互式 `Read-Host` 模型下载提示**，在 `hidden_popen` 隐藏 PowerShell 下会挂起 —— 属预存独立 bug
- **建议**：要么把该脚本 venv 也改为 `scripts\.venv`，要么废弃它（统一用 `install-env.ps1`）。需用户确认是否处理（本次未改���避免超出 wheel 纠正范围 + 规避 Read-Host 挂起风险）

## ✅ 已废弃并删除 install-uv-qinglong.ps1（用户"按照你推荐的顺序和方式进行"授权）
- 收敛理由：该脚本唯一非冗余能力均有替代 —— ① ace-step-ui 前端 setup.bat/setup.sh 已由 main.py 专用 _install_frontend_deps()（L5439 跑 npm install）覆盖；② 交互式 DiT/LM 模型下载走模型管理 UI，隐藏调用本就传 -SkipModels 跳过；③ 一键部署的 uv/venv/python_deps 步骤本就用 install-env.ps1。
- 操作：① 将该脚本的中国镜像/HF/CUDA/LFS 环境变量并入 install-env.ps1（L23 起：HF_ENDPOINT=hf-mirror.com、PIP_DISABLE_IP_VERSION_CHECK、GIT_LFS_SKIP_SMUDGE、CUDA_HOME=$env:CUDA_PATH）；② main.py 两处调用（_check_environment L3417、_install_uv L5255）改指 install-env.ps1 并移除 -SkipModels；③ 从 main.py 的 scripts_to_extract 列表（L7885）与文件头注释（L26）移除该脚本；④ git rm 删除 dev/app/scripts/install-uv-qinglong.ps1。
- 校验：install-env.ps1 PowerShell ParseInput -> ERRORS=0/SYNTAX_OK；main.py ast.parse 通过；全仓（除 BAK/ 与 build/_pyinstaller_work 旧缓存）已无对该脚本的活动引用（docs/ 引的是更古老的 1、install-uv-qinglong.ps1，本就失效）。
- 待交付：重建 exe（后台 build_light.py）后从 CArchive 提取实测确认已不含 install-uv-qinglong.ps1、且 install-env.ps1 含合并的 HF_ENDPOINT 等变量；随后 git commit。
## 📦 上传 flash_attn wheel 到仓库下载页（用户提供了 GitHub + 码云令牌）
- **GitHub（yunjii-cn/music）—— ✅ 成功且已验证可下载**
  - 建 release `tag=wheels`（release id 358609615，HTTP 201）
  - 上传 asset：`flash_attn-2.8.3+cu128torch2.9.0cxx11abiTRUE-cp312-cp312-win_amd64.whl`（asset id 487070799，HTTP 201，250,851,873 字节）
  - 下载地址 `https://github.com/yunjii-cn/music/releases/download/wheels/flash_attn-2.8.3+cu128torch2.9.0cxx11abiTRUE-cp312-cp312-win_amd64.whl` 实测 302→200、`Content-Length: 250851873`，文件名与 `install-env.ps1` 内写死地址**完全一致** → 一键部署维护现在能自动拉取 flash_attn（无需打包进 exe）
- **码云 Gitee（yunjii/music）—— ⚠️ 仅建好 release，wheel 没传上去**
  - 已建 release `tag=wheels`（release id 758333，默认分支 main）
  - **资产上传 API 从沙箱怎么试都 404**：`POST/GET /releases/{id}/assets` 与 `/attachments` 均 404（release 本体 GET 是通的）。Gitee 该端点在当前环境不可用（可能是企业版/版本差异），无法靠 guesses 解决
  - 待用户自行在 Gitee 网页端把 wheel 拖到 `wheels` release（30 秒），或改用 Git LFS 推文件
- **🔧 上传方式关键坑（可复用）**：本沙箱 **后台 Bash 任务使用隔离文件系统视图，curl 的 `@文件` / `--data-binary @` / `-T` / `-F file=@` 读文件全部失败**（报 "cannot open" / "error reading a file" / HTTP 000），但**前台**有文件读权限+网络。250MB 大文件最终用**前台 Python** 上传成功：`urllib.request` 以文件对象流式 POST（`data=open(path,'rb')`，GitHub raw octet-stream）；Gitee 需 multipart，用 `http.client.HTTPSConnection(...).request("POST", path, body=生成器, headers=...)` 以生成器流式发 multipart（避免 250MB 占内存）。token 用环境变量传入、不回显。
