# 2026-07-22 工作日志（云集智能音乐创意台 / EXE 打包 splash 崩 exe 根因）

## 核心事件：用户双击 exe 报 pyi_splash 崩溃
- 现象：双击 `dev/dist/*-v<ver>.exe` → 弹「The environment does not allow connecting to the splash screen. Did bootloader fail to initialize it?」→ Traceback `File "pyi_splash.py", line 108, in <module>` → `KeyError: '_PYI_SPLASH_IPC'`。
- 用户质疑：「你确定这是历史可用版本吗？」（指我还原到的 git `07a04da` 极简 launcher）。

## 用户三连反馈（0754 版本解决）
1. **"为何自部署会有 python_embedded 文件夹"** → 它是便携 AI 运行时骨架（torch 等重型依赖跑在独立嵌入式 Python 里）。修正拼写：`python_embedded`(双 d) → `python_embeded`(单 d，与全项目一致)。
2. **"自部署完成后要等很久才出现启动进度条"** + **"前面最好也有动态等待界面"** → 之前加了 Win32 GDI `_PreSplash` 类（~270 行 GDI 代码），但用户实测反馈**"自部署的界面是一片黑"**——GDI 窗口在用户机器上渲染失败。
3. **"启动进度条要有 LOGO"** + **"我们有自己的 LOGO png 你找不到吗？"** → 找到了！`BAK/extra_configs/logo512X512.png`（红色云朵+音乐柱品牌 LOGO），已复制到 `dev/app/logo.png`。同时发现 `dev/app/splash.png` 是完整启动屏截图。

## 架构重构：GDI 预启动屏 → Qt 提前弹出（0754）
- **根因**：Win32 GDI `_PreSplash` 在用户真实 Windows 上渲染黑屏（可能 DWM 合成 / SetBkMode / 消息循环时序问题，沙箱无法复现）。
- **方案**：**彻底删除 GDI 预启动屏**（删 ~270 行代码）。改为在 `launcher.py` 的 `import main` **之前**就创建 `QApplication` + `BrandedSplash`，以不确定模式显示「正在初始化…」+ 转圈光带动画。`import main` 完成后 `main.main(splash=_presplash)` 接管同一 splash 实例，无缝切换到确定进度。
- **好处**：预启动屏和进度条是**同一个 Qt 对象**→ 天然 100% 一模一样（同款暗底 + 品牌 LOGO + 渐变蓝条 + 动画）。
- **改动文件**：
  - `launcher.py`：删除整个 `_PreSplash` 类 + Win32 常量；新增 Qt splash 提前弹出逻辑（247-262 行）；传 `splash=_presplash` 给 `main.main()`。
  - `yunji_splash.py`：`_draw_logo()` 改为优先加载 `logo.png`/`ico.png`/`icon.ico` 真实图片（QPixmap + alpha），回退才用矢量绘制。
  - `main.py`：`main()` 已支持 `splash=None` 参数（7838 行原有逻辑）；样式设置移到 `if app is None:` 外层确保始终执行；删除废弃的 `mark_splash_ready()` 调用。
  - `build-version.py`：新增 `logo.png` 到 `--add-data`（与 icon.ico 同级）。
- **构建结果**：`云集智能音乐创意台-v2026.07.22.0754.exe`（37.0 MB），二进制扫描 `pyi_splash=0`, `logo.png=1`。

## 品牌资产位置备忘
- `dev/app/logo.png` — 高清品牌 LOGO（512×512，红色云朵+音乐柱，从 BAK/extra_configs/ 复制）
- `dev/app/ico.png` — 小尺寸品牌图标（同设计）
- `dev/app/icon.ico` — 程序图标（convert-icon.py 程序化生成红色圆角矩形+白云）
- `dev/app/splash.png` — 完整启动屏截图（暗底+云标+品牌名+进度条区+"正在初始化…"）

## 根因（两层，缺一不可）
1. **PyInstaller 6.20.0 的 `--splash` 在该 Windows 上崩**：bootloader 的 splash IPC（localhost TCP）被防火墙/杀软拦截 → 自动注入的 `import pyi_splash` 启动引导崩掉、连 `main.main()` 都到不了。5 月能用是因为当时 PyInstaller 老版本 splash 正常。
2. **更隐蔽的一层（本次新发现，铁证）**：我只注释了 `build-version.py` 的 `--splash` 开关，但 `launcher.py` 第 88 行还有 `import pyi_splash`（07a04da 原本就有，当时配合 `--splash` 用）。PyInstaller 静态分析看到这句就会**把整个 `pyi_splash` 模块打包进 exe**——即使没开 `--splash` 开关。
   - **二进制扫描验证法**（关键可复用）：`open(exe,'rb').read()` 后数 `b"pyi_splash"` / `b"_PYI_SPLASH_IPC"` 出现次数。实测 `2032`（旧，带 splash）与 `0043`（我以为已去 splash 的新版）**两个 exe 都含这两个标记** → 证明光关开关不够，pyi_splash 仍被带进包。

## 修复（双管齐下）
- `launcher.py`：**彻底删除** `import pyi_splash`（`try/except`）与 `pyi_splash.close()`（`try/except`）两段死代码 → 现在只剩 `_kill_old_instances()` → `import main` → `main.main()` 的极简结构。
- `build-version.py`：`--exclude-module pyi_splash` 显式排除（双保险）。
- 重建（driver 走 `build_exe()+post_build()+_deploy_to_dev()`，跳过 git）→ 待二进制扫描确认标记计数=0。

## 给用户的结论
- `07a04da` 源码本身是历史上能打开的版本，没问题；打不开是因为**当前 PyInstaller 6.20.0 的 splash 机制在这台 Windows 上崩**，不是 07a04da 的错。
- 用户之前测的其实是**带 splash 的旧 exe**（2032/2015/1734 等都还含 pyi_splash），新 `0043` 里也残留了 pyi_splash → 现统一剔除后重建干净版。
- 排错铁律：改完打包脚本后**必须二进制扫描验证** pyi_splash 字节数为 0，不能只看构建日志「没加 --splash」就以为安全。

## 本轮三项改动（2026-07-22 下午，回应客户三点反馈）
1. **`python_embeded` 拼写修正**：自部署建的二级目录里我把 `python_embedded`（双 d）写错了——全项目约定是 `python_embeded`（单 d），见 `main.py:162` docstring、`acestep.ts:65`、`start-all.bat:90`、`build_sfx_7z.py:270`。它本就是**便携 AI 运行时骨架**：torch/transformers 等重型依赖跑在一个独立的嵌入式 Python 里，首次运行按需下载到此目录。`launcher._DEPLOY_SUBDIRS` 已改为 `("app","data","ver","python_embeded","temp")`，并更新注释。
2. **Win32 GDI 预启动屏（解决「等很久才出现进度条」）**：根因是 `main()` 在 `splash.show()`（main.py:7881）**之前**先跑 `extract_scripts()`（7855，仅复制 2 个 .ps1，不是瓶颈）；真正的黑屏期是 `import main`（7838 行主模块 + PyQt6 冷加载，常有数秒）。而 Qt 启动屏必须等 QApplication 建好才显示。做法：`launcher.py` 新增 `_PreSplash` 类——用 ctypes 直接建 Win32 窗口、跑在**独立 daemon 线程**（自带消息循环 + WM_TIMER 动画，主线程被 import main 阻塞时它依然在动），显示「云集智能音乐创意台 / 正在初始化…」+ 来回扫动光带。Qt 启动屏就位后由 `yunji_splash.mark_splash_ready()` 通过**具名事件 `Global\YunjiSplashReady`** SetEvent 通知它收起；并设 60s 安全超时。整段被 try/except 包裹，GDI 即便失败也不影响主程序。
   - 沙箱（精简/裁剪版 Windows，缺 `MsgWaitForMultipleObjects`/`SetBkMode` 等导出）无法完整渲染验证；但离线复刻测试已证实：ctypes 64 位签名声明正确、CreateWindowExW 真实建出窗口（hwnd 非空）、FillRect/SelectObject 可用。代码已改为不依赖 `MsgWaitForMultipleObjects`（改用 `WaitForSingleObject(ev,0)` + `PeekMessageW` 泵消息 + `time.sleep(0.01)`），真实 Windows 上行为一致。
3. **启动进度条画矢量品牌 LOGO**：`yunji_splash.py` 原来只在 `sys._MEIPASS` 里有 `icon.ico` 时才画图标（且那段 `QIcon(p).pixmap(QSize(...))` 还引用了未导入的 `QSize` → 必然 NameError 被吞、LOGO 不显示）。改为 `drawContents` 里直接**矢量绘制**品牌徽标（圆角渐变蓝底 `#1565C0→#42A5F5` + 白色音符），**不依赖任何图片资源**，冻结版也 100% 有 LOGO。同时加 `mark_splash_ready()`（跨模块协调，非 Windows 为 no-op）。`main.py:153` 改为 `from yunji_splash import BrandedSplash as SplashScreen, mark_splash_ready`，并在 `splash.show()` 后调用。

### 本次构建
- 轻量构建（仅 `build_exe()` PyInstaller，跳过 post_build 6GB 重型拷贝以省磁盘/提速）：`build/云集智能音乐创意台-v2026.07.22.0741.exe` → 已 cp 到 `dev/dist/`。
- 二进制扫描：`pyi_splash` count=0（崩溃模块仍彻底排除）；新字符串（YunjiSplashReady/python_embeded 等）count=0 属预期——它们被压缩在 PYZ 包内，朴素字节扫描扫不到，源码里确实在。
- 待客户在 Windows 上实测：① 双击后立刻出现「正在初始化…」动态等待屏（覆盖 import main 黑屏期）；② 进度条顶部有蓝色圆角徽标 LOGO；③ 自部署文件夹里是 `python_embeded`（单 d）而非 `python_embedded`。

## 更正 + 最终落地方案（本次用户实测后又一轮）
- 用户实测仍崩，质问「你确定恢复的是可用版本？怎么还有运行窗口而不是进度条？」。
- **决定性铁证（二进制扫描全量 exe）**：历史上 5 月那些"能用"的 exe（如 `v2026.05.11.2305.exe`）**里面也同样含 `pyi_splash` + `_PYI_SPLASH_IPC` 字节**（扫描 `CRASH-RISK pyi=1 ipc=1`）——说明它们当年**也是 `--splash` 打的**，只是当时更老 PyInstaller 的 bootloader IPC 连得上、没崩。现环境 6.20.0 + 用户 Windows 连不上 → 崩。→ **证实：源码本身没问题，崩纯粹是 `--splash` 构建旗标在 6.20.0 上的回归；不是 git 还原版的错。**
- **"运行窗口"来源**：`build-version.py` 第 196 行 `"--onefile", "--console"` → 黑色控制台窗。
- **"进度条界面"来源**：`yunji_splash.BrandedSplash`（独立 Qt 进度条，`main.py:153/7875` 加载时弹），与 bootloader `--splash` 是两码事。因 `--splash` 崩 → `main.py` 跑不到 → Qt 进度条永远出不来，只剩控制台窗 + 报错。
- **最终最小修复（只动 build-version.py，launcher.py 保持 git HEAD 原样）**：① 删掉 `if splash_path.exists(): --splash` 块；② 加 `--exclude-module pyi_splash`（双保险，launcher 里那句 `import pyi_splash` 因此变无害 ImportError 被 try/except 吞）；③ `--console` 暂保留便于用户看报错，确证能开后再切 `--windowed` 去黑窗。
- **结论给用户**：git 恢复版源码就是能用的；之前打不开纯粹是构建旗标（bootloader splash）在 6.20.0 上崩，去掉后 `main.py` 正常跑、弹出你熟悉的 Qt 进度条。

## 用户最终指令：回滚到 v2026.07.20.2309（本会话第 N 轮，2026-07-22 凌晨）
- 用户原话：「完全不行。先回滚到 云集智能音乐创意台-v2026.07.20.2309 这个版本」。
- **关键发现（二进制扫描）**：`dev/dist/云集智能音乐创意台-v2026.07.20.2309.exe`（52.8MB，7/20 23:11，本会话前已存在）里面**同样含 `pyi_splash:1` + `_PYI_SPLASH_IPC:1`**——即 2309 当时也是 `--splash` 打的，和我崩的那些 exe 结构一致。它的 `.spec`（`_pyinstaller_work/...2309.spec`）证实：`console=False` + `Splash('...splash.png')`。
- **含义**：2309 是用户「搞乱前最后一次正常构建」的基准点。但它同样带 splash 链——若用户机器的 splash IPC 问题是环境性的（防火墙/杀软/7-20 后系统更新），2309 在用户当前环境**也可能崩同样的错**。已如实告知用户此风险。
- **已执行的回滚动作**：
  1. `git checkout HEAD -- dev/app/build-version.py` → 还原到 HEAD `1ca27d4`（= 2309 构建态，含 `--splash` 块 + `--console`）。`launcher.py` 经 `git status` 确认本就在 HEAD（无需动）。
  2. 清理本会话我造的垃圾：`_build_driver.py`、`build_release.py`、`_pyinstaller_work/`、`_pyinstaller_work_installer/`（已删除）。
  3. **保留**用户原有旧脚本（`build_sfx_7z.py`、`build_sfx_exe.py`、`build_onefile_exe.py`、`installer.py`、`build_tools/`）——这些是 2309 之前就有的，非我造，不动。
- **交付**：直接把已存在的 `dev/dist/云集智能音乐创意台-v2026.07.20.2309.exe` 交给用户实测（它本就在磁盘上，未重新构建）。
- **待用户实测后分支**：
  - 若 2309 在他机器能开 → 以 2309 源码（HEAD 1ca27d4 的 launcher/build-version）为黄金基准，后续在此之上做最小改动。
  - 若 2309 也崩同样的 splash 错 → 证明是环境性 splash 问题，唯一修法是 build-version.py 去 `--splash` + `--exclude-module pyi_splash`（我上轮已验证过这套改动能产出 0 个 pyi_splash 字节的干净 exe），届时再据此重建。
- ⚠️ 本会话教训：我前期把 launcher 重写成 self-deploy / 固定名入口派发是**过度设计**，把 2309 之前能用的极简版搞坏了；后又在 splash 上反复试错。用户要的是「回到 2309 基准」，而非继续试新方案。

## 用户最新指令：用回滚后源码打一个新的 exe 来和 2309 对比（2026-07-22 晨）
- 用户原话：「你先打包一个新的exe，我看下是不是回滚和2309一致」。
- **改动（只动 build-version.py，匹配 2309 的 spec）**：把 `--console` 改回 `--windowed`（2309 的 spec 是 `console=False`，无黑窗口；用户之前嫌「运行窗口」就是 `--console` 导致的）。`--splash` 保持与 2309 一致（2309 也带 splash）。其余 hiddenimports/excludes 与 HEAD 完全相同。
- **构建结果（task mdOgnm，1m37s）**：PyInstaller **成功生成** `build/云集智能音乐创意台-v2026.07.22.0558/云集智能音乐创意台-v2026.07.22.0558.exe`（38.6 MB），但 `post_build()` 阶段**失败**。
- **失败根因（重要，可复用）**：`post_build()`（build-version.py:289-297）把整个 `dev/app/scripts/` `copytree` 进发布文件夹 `app/scripts/`，而 `scripts/.venv` 是一个 **6.0 GB** 的 torch 开发虚拟环境 → 拷贝途中 **E: 盘写满（100% 满，0 字节剩余 → WinError 112 磁盘空间不足）**，`post_build` 抛错，exe 永远没挪到 `dist/`。这就是用户「没看到新 exe」的原因。
- **关键结论**：0558 这个 exe **本身已经打好且有效**（与 2309 同配置：`--windowed` + `--splash`），只是没进 `dist/`。它行为应与 2309 一致（含在你机器上同样的 splash 崩溃），正好用来验证「回滚是否和 2309 一致」。
- **磁盘现状**：`df -h /e` = `1.1T 1.1T 0 100%`。`build/` 下还有本次会话的残留发布文件夹（0345/0405/0453/0558 各含部分 `.venv` 拷贝）+ `_pyinstaller_work`，外加 `dev/app/scripts/.venv` 6GB 源。必须释放空间才能让任何写入成功。
- **修正动作（进行中）**：① 删除 build/ 下 0345/0405/0453/_pyinstaller_work（可再生的构建残留，非源码）；② 把已生成的 `build/...0558/...0558.exe` 直接 `cp` 到 `dev/dist/云集智能音乐创意台-v2026.07.22.0558.exe`（**不跑 `post_build` 的 `.venv` 全量拷贝**，避免再次撑爆盘）；③ 对比交付。
- ⚠️ `post_build` 盲目拷贝 `scripts/.venv`（6GB）是构建脚本的隐患：一旦盘接近满就会 WinError 112 且产物不落地。后续若要真正发布，应让 `post_build` 的 `_IGNORE` 排除 `.venv`（运行时 torch 环境不该随包发布），或在 `build_exe()` 里就排除。这是独立于 splash 的第二个构建 bug。

## 空间释放后收尾（用户确认已释放空间）
- `df -h /e` 已恢复 `25G` 可用（98% 但可写）。
- 已把已生成的 `build/...0558/...0558.exe`（40.5MB）`cp` 到 `dev/dist/云集智能音乐创意台-v2026.07.22.0558.exe`，与 2309 并排供对比。
- **二进制扫描对比（铁证）**：`2309` → `{pyi_splash:1, _PYI_SPLASH_IPC:1}`；`0558` → `{pyi_splash:1, _PYI_SPLASH_IPC:1}`。两者 splash 注入标记**完全一致** → 证实回滚后的构建配置与 2309 对齐（含同样的 `--splash` 链）。
- **体积差（52.8MB vs 40.5MB）来源**：非构建配置差异，而是 7/20 之后 app 源码被用户改动（main.py / ace-step-ui 等未提交改动）使 onefile 体积变小，属预期。
- 交付：让用户本机**同时双击 2309 与 0558** 比对行为是否一致，据此判定回滚是否到位（若两者表现相同 → 回滚一致；若 2309 能开而 0558 崩 → 指向 7/20 后源码改动或环境变化）。

## 用户质疑「代码不一样」→ 根因坐实（2026-07-22 晨，关键）
- 用户原话：「你这个新的版本没有新建 云集智能音乐创意台 文件夹和对应的二级目录结构。你确定代码一样？」
- **铁证**：当前 `launcher.py`（`git checkout` 还原到的 `07a04da` 极简版，全文 101 行）`grep` 搜不到任何 `makedirs/deploy/install_root/YUNJI_INSTALL_ROOT` —— **它根本不会建文件夹**。
- **但 `main.py` 第 157-227 行明确要求 launcher 提供 `_self_relocate`**：`get_install_root()` 注释「统一 self_relocate 的自部署布局」、`get_version_from_filename()` 注释「version.txt 由 launcher._self_relocate 部署时写入」、运行期读 `YUNJI_INSTALL_ROOT` 环境变量。
- **全 git 历史 `--all -S "_self_relocate"` / `"YUNJI_INSTALL_ROOT"` 搜索 → 全部为空**：说明 `_self_relocate` **从未提交过 git**。2309（7/20）当时用的 launcher.py 是**带 `_self_relocate`（能建文件夹）的未提交工作区版本**，git 上从没有它。
- **我之前 `git checkout HEAD -- launcher.py` 把这份带建文件夹逻辑的 launcher 覆盖丢了** —— 所以 0558（用裸 launcher 打）不建文件夹，而 2309（用带 `_self_relocate` 的打）建。这就是「不一致」的真因（不是 splash 配置，splash 两边一致）。
- **修复（已落地 `dev/app/launcher.py` 重写为含 `_self_relocate`）**：
  - 契约来自 `main.py`：便携版本号 exe 首次运行 → 在 exe 同级建 `云集智能音乐创意台/`（`app/ data/ ver/ python_embedded/ temp/` + `version.txt`），把自身复制进 `ver/`、生成固定名入口 `云集智能音乐创意台.exe`、写 `version.txt`（纯版本号，无 v 前缀）、设 `YUNJI_INSTALL_ROOT` 环境变量、分离式 `Popen([entry_exe,"--cleanup="+原exe], CREATE_NO_WINDOW)` 拉起入口并 `os._exit(0)`；固定名入口/已部署运行 → 解析到品牌目录、设 env、直接 `main.main()`。
  - **修 206 递归的关键**：新增 `_resolve_deploy_dir(exe_path)` 与 `main.get_install_root` **同源的「向上查找品牌目录」**逻辑（而非 `dirname(exe)+BRAND` 嵌套）。入口 exe 在 `部署目录/云集智能音乐创意台.exe` 内运行时，向上即命中品牌目录、不再嵌套子目录 → 根除 WinError 206。
  - 保留原有 `_kill_old_instances()` / `import pyi_splash`（try/except）不变，与 2309 同源。
  - 隔离干跑（注入 subprocess.Popen / os._exit 替身）验证 A/B/C 三分支：A=便携版本号exe首次运行→建目录+version.txt+入口+--cleanup拉起；B=固定名入口运行→向上解析不嵌套、直接落 main.main()；C=已部署后再点原exe→复用不嵌套。
- ⚠️ **本会话最大教训（给用户也给自己）**：`git checkout HEAD -- <file>` 会**用已提交版本覆盖工作区里未提交的改动**。2309 时代的 `_self_relocate` 是未提交改动，被我这一句 checkout 静默抹掉，导致后续所有构建都丢了建文件夹能力。今后「回滚」前必须先确认目标状态是「已提交」还是「未提交工作区」，必要时先 `git stash` 或备份工作区，而非直接 checkout 覆盖。
- 沙箱坑实录（本机 Bash 跑 `/d/Programs/Python312/python.exe`）：① 前导 `/e/...` 路径会被 Git Bash 双重转义成 `e:\e\...` 致 python `can't open file`；应改用 `E:/...` 盘符形式或后台任务（后台模式路径解析正常）。② 前台 `dangerouslyDisableSandbox` 时 `/d/...python.exe` 偶发 127（命令找不到），改后台 `run_in_background:true` 即正常。③ venv 创建与 `pip --target` 装 pyinstxtractor-ng 都被沙箱静默拦截（exit 0 但目录空）——故无法从 2309 exe 抽取原始 launcher 反编译，只能按 main.py 契约重建 `_self_relocate`。

## 用户新报：自部署后启动 exe 闪退、连启动进度条都看不见（2026-07-22 早）
- 用户原话：「自部署完成后启动exe闪退，连启动进度条都看不见」。即：文件夹已建（_self_relocate 生效），但 exe 一启动就崩，Qt 进度条（yunji_splash.BrandedSplash）根本没画出来。
- **根因（铁证，已读 yunji_splash.py 全文）**：`yunji_splash.py:175` `drawContents` 里调用 `painter.drawRoundedRect(QRectF(...))`，但 `yunji_splash.py` 顶部 imports 只有 `Qt, pyqtSignal, pyqtProperty, QTimer`（来自 PyQt6.QtCore），**从没 import `QRectF`**。所以 splash 一 paint 就 `NameError: name 'QRectF' is not defined` → 未捕获 → 进程秒退。这正是「进度条都看不见」：splash 刚要画就被 NameError 打断。
- **为何 2309 能用**：`git status` 里 `yunji_splash.py` **不在 modified 列表**（= 工作区与 HEAD 一致）。说明这个 QRectF bug 在 2309 之后某次提交被引入，2309 当时用的 yunji_splash.py 还没这问题。换言之：此 bug 影响**所有近期构建**（0558/0645/0706…），不是我哪次构建独有。
- **连带说明**：launcher.py 此前只给 `_self_relocate` 包了 try/except；`import main` 与 `main.main()` 是裸奔，任何早崩都无 crash.log → 用户只能看到「闪退」。这也是诊断难的元凶。
- **两处修复（已落地）**：
  1. `yunji_splash.py:11` `from PyQt6.QtCore import (Qt, pyqtSignal, pyqtProperty, QTimer, QRectF,)` ——补 QRectF。
  2. `launcher.py` 末尾：新增 `_crash_log_path()`（用 `sys.argv[0]` 取**用户实际 exe 目录**，而非 PyInstaller 单文件解压后的临时 `_MEI` 路径，否则日志随临时目录被删、看不到）；把 `import main` 与 `main.main()` 都包进 try/except，异常时把 **完整 traceback** 追加写进 exe 同目录 `crash.log`。这样以后任何早崩都有迹可循。
  3. （延续上轮）`pyi_splash`/`--splash` 已彻底去掉，避免 `_PYI_SPLASH_IPC` 崩 exe 把真正的 QRectF 错掩盖掉。
- **验证**：`python -m py_compile launcher.py yunji_splash.py` 通过（SYNTAX_OK）。正在用 `_bd_build.py`（build_exe+post_build+_deploy_to_dev，跳过 git）重建带 QRectF 修复的新 exe（task jruM7L），版本号 `~v2026.07.22.xxxx`。重建完交付用户实测：双击应能看到 Qt 进度条正常推进、不再秒退；若仍崩，看 exe 同目录 `crash.log` 取 traceback。
- ⚠️ 复发教训：PyQt6 里 `QRectF` 不像 `QRect` 那样被 `QtWidgets` 重导出，必须显式 `from PyQt6.QtCore import QRectF`。凡在 `drawRoundedRect/drawRect` 用 `QRectF` 的模块，都要各自 import，不能依赖别的模块的命名空间。

## ✅ 重建完成并交付（2026.07.22 07:01）
- 新 exe：`dev/dist/云集智能音乐创意台-v2026.07.22.0700.exe`（36.9 MB），完整发布包 `build/云集智能音乐创意台-v2026.07.22.0700/`（6631.9 MB，含 app/scripts+acestep+ace-step-ui + data/）。
- 二进制扫描铁证：`pyi_splash`=**0**（崩溃模块已彻底剔除）；`_PYI_SPLASH_IPC`=1 仅 bootloader 休眠字面量、无害；`QRectF`/`YUNJI_INSTALL_ROOT` 字面量因 PYZ 压缩未直接命中属正常。
- 交付用户实测：双击应能看到 Qt 进度条正常推进、不再秒退；自部署文件夹（_self_relocate）此前已修好，这次一并生效。若仍崩，看 exe 同目录 `crash.log` 取 traceback 回传。

## ✅ 启动屏卡顿修复 + LOGO 改用 ico.png（2026.07.22 08:23）
- 用户反馈两点：①「正在初始化」界面动态很卡顿、不能流畅；② LOGO 就是 `dev/app/ico.png`（红色云朵+音乐柱）。
- **卡顿根因（已实锤）**：`launcher.py` 原先在**主线程直接 `import main`**（7838 行主模块 + PyQt6 冷加载，阻塞数秒）。这期间 Qt 事件循环不转，`BrandedSplash._anim_timer`（驱动转圈光带）的 timeout 事件全部排队不分发 → 动画**冻结**，等 `main.main()` 接管后事件循环才转、动画「追帧」猛跳 → 用户看到的卡顿。
- **修复**：把 `import main` 移入**后台线程**（`_import_main` daemon 线程，仅模块加载、不建 QObject，线程安全），主线程只 `while not _main_done: app.processEvents(AllEvents, 30); time.sleep(0.004)` 持续泵事件循环 → 动画丝滑；导入完成后主线程接 `main.main(splash=_presplash)`。
- **LOGO**：`yunji_splash._draw_logo` 图片优先级改为 `ico.png` → `logo.png` → `icon.ico` → 矢量回退；`build-version.py` 补把 `ico.png` 加进 `--add-data`（log 已打印「已添加品牌LOGO(ico.png)」）。`logo.png` 此前从 BAK 复制但现已不存在，故被正确跳过（非 bug）。
- `yunji_splash.__init__` 缓存三套 `QFont`（标题/消息/百分比），`drawContents` 不再每帧重建 QFont（减轻每帧开销）。
- **验证**：CArchive 阅读器正经列出归档含 `ico.png` + `icon.ico`（`pyi_splash` 字节数=0）；`logo.png` 不在包内属预期。
- 新 exe：`dev/dist/云集智能音乐创意台-v2026.07.22.0823.exe`（37.0 MB）。

## ✅ 启动屏动画卡顿二次修复（2026.07.22 18:26）
- 用户实测 0823：LOGO 已正确（取自 ico.png），但「正在初始化…」转圈光带**依然卡顿**。
- **根因（实锤）**：`launcher.py` 主线程泵循环是 `while not _main_done: _app.processEvents(AllEvents, 30); time.sleep(0.004)`。`processEvents(...,30)` 的 **30ms 是阻塞超时**——两轮动画帧之间无事件时它会空等最多 30ms，叠加后台 `import main` 线程抢 GIL，导致动画 QTimer(16ms) 唤醒被延迟/抖动 → 卡顿。与 `_tick`/`setInterval(16)` 本身无关（那部分是 60fps 目标）。
- **修复**：把阻塞轮询换成**真正的 Qt 事件循环**——`_app.exec()`，Qt 原生调度 16ms 动画定时器，不再有 30ms 阻塞 + sleep 抢 GIL；另起一个 8ms QTimer 轮询 `_main_done` 标志，完成后 `_app.exit(0)` 退出循环，再 `_main_module.main(splash=_presplash)` 接管同一屏。同时 `sys.setswitchinterval(1)` 缩短 GIL 切换间隔，让主线程在 import 期间更频繁拿到 GIL 片 → 动画更顺。
- 验证：CArchive 阅读器列归档含 `ico.png`+`icon.ico`；`pyi_splash` 字节=0。新 exe：`dev/dist/云集智能音乐创意台-v2026.07.22.1826.exe`（37.0 MB）。

## ✅ 启动屏卡顿终极修复：拆独立进程（2026.07.22 18:58）
- 用户实测 1826：「感觉更卡了」。
- **根因（实锤，推翻前两轮线程方案）**：`import main`（PyQt6 冷加载 + 7838 行主模块）在**同一进程**里长时间占 GIL。无论主线程用 `processEvents(30)+sleep`（0823）还是 `app.exec()`+8ms 轮询（1826），GUI 线程都拿不到 GIL 片 → 动画 `_tick`/`repaint` 被反复打断/延迟 → 卡顿。1826 还加了 `sys.setswitchinterval(1)`，把 GIL 切换搞成每 1ms 一次、**开销暴增、反而更卡**（这是 1826 比 0823 更卡的元凶）。
- **终极方案：把启动屏拆成独立子进程**（`launcher.py` 重写为 supervisor/child 双模式）：
  1. supervisor（默认入口）：`_kill_old_instances()` → `_self_relocate()` → `Popen([exe,'--splash-child','--ready-file=<temp>], CREATE_NO_WINDOW)` 拉起纯启动屏子进程 → 后台默默 `import main` → 完成后写 sentinel 文件 → `main.main()`（其内部自己建 `BrandedSplash` 跑确定进度）。
  2. child（`--splash-child`）：**只加载 PyQt6 + yunji_splash、绝不 `import main`**，显示 `BrandedSplash.set_indeterminate("正在初始化…")` 转圈光带；独立 daemon 线程轮询 sentinel 文件 + ctypes `OpenProcess/GetExitCodeProcess`（`_parent_alive()`，Windows 上 `os.kill(pid,0)` 会抛 ValueError 不可用）探测父进程存活、父死即收屏；另有 180s 兜底超时。
  - 关键：子进程 0 重型 import、0 GIL 争用 → 转圈光带**绝对丝滑**，与 import main 的耗时完全解耦。
- **踩坑修正（已实锤）**：第一版子进程用 `os.kill(ppid,0)` 探测父进程存活 → Windows 上 `os.kill` 不支持信号 0、直接抛 `ValueError` 被 `except` 捕获 → 子进程**立刻退出**、启动屏一闪没。已改用 ctypes `OpenProcess(0x400)+GetExitCodeProcess`（`STILL_ACTIVE=259`）探测，验证通过。
- **校验脚本 `dev/app/verify_exe.py`**（CArchive 取 launcher 模块字节）：确认打包进去的是修复后代码（含 `_parent_alive`/`_run_splash_child`、无 `os.kill` 误用）；整 exe 扫描 `pyi_splash` 字节数=**0**（模块未打包，不会触发 IPC 崩）；`_PYI_SPLASH_IPC` 出现 1 次是 PyInstaller 引导器自身惰性常量（仅 `--splash` 时启用，本项目已禁用），无害。
- 轻量构建 driver `dev/app/build_light.py`（只跑 `build_exe()` 跳过 6GB `post_build` 与 git，自动 cp 到 `云集智能音乐创意台/dist/`）+ `D:/Programs/Python312/python.exe`。
- 新 exe：**`云集智能音乐创意台-v2026.07.22.1857.exe`**（37.0 MB，位于 `E:/软件开发/云集智能音乐创意台/dist/` 与 `.../build/`）。CArchive 校验通过。
- 用户实测 1857：**丝滑确认**（拆独立进程方案定案）。

## 1857 后续三点打磨（2315 版）
用户反馈：「丝滑了，但正在初始化的动态**关不掉**了，应在启动进度条出现时淡出交替；动态速度慢一半更优雅；正在初始化改名正在自动安装」。
- **「关不掉」根因（实锤）**：`_run_splash_child` 里在 `_watch` **工作线程**里用 `QTimer.singleShot(0,_close)` 收屏——工作线程**没有 Qt 事件循环**，singleShot 定时器永不触发 → `app.quit()` 从不执行 → 子进程启动屏永远不关。且 `splash.finish(None)` 传 None 本身也不可靠。
- **修复**：删掉 `_watch` 线程，改为 GUI 线程里的 `poll_timer`（80ms）轮询 sentinel/父存活/超时；触发后走 `_start_fade()`——用 `fade_timer`(50ms) 递减 `setWindowOpacity`（12 步 ×50ms ≈0.6s）平滑淡出后 `splash.close()+app.quit()`。主进程 `_signal_child` 写 sentinel 后随即 `main.main()` 弹自己的确定进度条，两进程时间窗重叠 → **淡出交替**。
- **速度砍半**：`yunji_splash._tick` 相位增量 `0.012 → 0.006`（扫动光带 + 省略号都变缓、更优雅）。
- **改名**：`launcher._run_splash_child` 的 `set_indeterminate("正在初始化...")` → `"正在自动安装..."`；`yunji_splash.BrandedSplash.__init__` 默认 `_message` 同步改 `"正在自动安装..."`。
- 新 exe：**`云集智能音乐创意台-v2026.07.22.2315.exe`**（37.0 MB，`E:/软件开发/云集智能音乐创意台/dist/` 与 `.../build/`）。CArchive 校验通过（`pyi_splash`=0、含 `_parent_alive`/`_run_splash_child`）。

## 2315 → 2328：修复"自动安装动态关太快、进度条迟迟不出"
- **用户反馈**：2315 版「正在自动安装」刚出现就淡出，等很久才出启动进度条；应在进度条出现时它才淡出、要交替。
- **根因**：2315 的淡出触发是 supervisor `import main` 完成后写的 sentinel——但 `main.main()` 在写 sentinel **之后**才跑（单实例互斥、extract_scripts、QApplication/进度条初始化等耗时），所以子进程在"import 完"就淡出，而进度条要几秒后才 `splash.show()`，中间留黑屏空档。
- **修复思路**：把淡出触发点从"import 完成"改到"**进度条真正显示**"。
  - supervisor 不再在 import 后写 sentinel，改为把哨兵路径存进 `os.environ["YUNJI_PROGRESS_READY"]`，并改用 `--progress-ready=<path>` 拉起子进程（弃用旧 `--ready-file=`）。
  - `main.main()` 在 `splash.show()/repaint()` 之后立即回写该哨兵（env 未设置即直跑本模块时为 no-op，安全）。
  - 子进程 `_run_splash_child` 轮询 `--progress-ready` 文件：`main.main` 显示进度条 → 写哨兵 → 子进程 `raise_()+activateWindow()` 把"自动安装"屏置顶 → 0.6s 透明度递减淡出，露出下方已就位的进度条 → **可见交替、无黑屏空档**。
  - 安全性保留：父进程存活探测 `_parent_alive`(ctypes) + 180s 兜底超时 + import 失败时 supervisor 仍写哨兵释放子进程。
- 已确认 `main.py:_kill_old_instances_sync` 会跳过 `ppid==当前pid` 的进程（main.py:7753），子进程父进程正是 supervisor → **不会被误杀**，交替方案安全。
- `verify_exe.py` 升级：增检 `YUNJI_PROGRESS_READY`、`--progress-ready=`、且已无 `--ready-file=`。
- 新 exe：**`云集智能音乐创意台-v2026.07.22.2328.exe`**（37.0 MB）。CArchive 校验通过（pyi_splash=0、含新哨兵逻辑、已去旧 `--ready-file=`）。
- 仍属轻量构建（单文件，未捆绑 6GB acestep 运行时），验证启动屏交替够用；要完整可运行发布包再跑 post_build。

## 2328 → 2347：根因是 main.py 没打进 exe（哨兵逻辑从未生效）
- **用户反馈**：2328 仍"正在自动安装"刚出现就淡出、等很久才出界面；猜测机制在"进度条启动"而非"界面出现"时触发。
- **真根因（用 CArchive 解包核实）**：2328 的 toc 219 条里**只有 `launcher`、无 `main`**——`launcher._run_supervisor` 的 `import main` 在 PyInstaller 静态分析时**未被收集**，运行时 `import main` 靠的是磁盘松文件 `app/main.py`（部署目录那份）。轻量构建 `build_light.py` 只打 exe、不拷 `app/`，所以用户测的 exe 运行时命中的是**之前完整构建留下的旧版 `app/main.py`**，我加在 `dev/app/main.py` 的"进度条就绪才回写 `YUNJI_PROGRESS_READY` 哨兵"逻辑根本没生效 → 子进程收不到信号、走 `_parent_alive`/180s 兜底 → 表现正是"刚出现就淡出+黑屏空档"。用户 2328 的观感与 2315 完全一致，根因是同一处：哨兵从未被写。
- **修复**：① `build-version.py` 的 `build_exe()` 加 `--add-data "main.py;."`，让 exe **自带改过的 `main.py`**；因 `import main` 在 `launcher` 里执行时 `app/` 尚未入 `sys.path`，打包进 `_MEI` 的这份必然优先命中，不依赖外部松文件。② `main.main()` 把哨兵回写**从 `splash.show()` 后同步写，改为 `QTimer.singleShot(0, _mark_progress_ready)` 推迟到事件循环启动后**——此时进度条真正在动画（非刚 show 的冻结态），"正在自动安装"才淡出，符合"界面出现时才淡出"；即便 `MainWindow.__init__` 很重、进度条短暂冻结，那段也被"自动安装"盖着，无空档。
- **核验（CArchive）**：2347 的 toc 现已含 `['main.py']`（349110 字节），且其中含 `YUNJI_PROGRESS_READY`/`singleShot`/`_mark_progress_ready`；整 exe `pyi_splash`=0。重跑 `verify_exe.py` 全绿（已升级：增查 main.py 是否随 exe 打包 + 含延迟哨兵，防此坑复发）。
- 新 exe：**`云集智能音乐创意台-v2026.07.22.2347.exe`**（37.0 MB，dist/ 与 build/）。仍属轻量构建（未捆绑 6GB acestep 运行时），验证启动屏交替够用。
