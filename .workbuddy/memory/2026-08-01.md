# 2026-08-01 工作日志

## 首跑「删除自身」终极修复（exe v2026.08.01.0019）
- **现象**：2359 版首跑能显示启动器，但不删除原始便携 exe。根因：2359 用 `MoveFileEx(MOVEFILE_DELAY_UNTIL_REBOOT)` 重启删除，**需管理员权限**，非管理员下静默失败（`_safe_delete` 才是 `os.remove` 带重试、无需管理员）。另：当前首跑分支 `_ensure_entry_current` 在 `ver/` 为空时返回 None，入口从未生成（隐患）。
- **方案（双保险，恢复 git 历史 f7d9105 语义 + 防黑屏兜底）**：
  1. **首选（恢复历史 spawn 机制）**：首跑 spawn 固定名入口 entry 带 `--cleanup=<原始便携exe>`；entry 显示后 `_safe_delete(cleanup_target)` 立即删（os.remove，无需管理员）。加**看门狗** `_wait_entry_ready`：spawn 后等 ~4s 确认 entry 存活才 `os._exit(0)`；若被杀软杀掉则 supervisor 自己 fall-through 显示（保显示不黑屏）。
  2. **兜底（杀软拦了 spawn 也能删）**：首跑时把原始 exe 绝对路径写到 `部署目录/.cleanup_pending`；下次以已部署态启动时（文件已不在运行）读它 `os.remove`（无需管理员、不触发杀软）。另在 fallback 显示后派生 `--self-delete-helper` 助手（同 exe，DETACHED）：等 supervisor 退出后删原始 exe——显示后才派生，被杀只损「立即删」、不损显示；被杀则靠 .cleanup_pending 下次启动兜底。
- **入口创建修正**：首跑分支恢复「生成 entry（_ensure_entry_current）+ 复制自身进 ver/」（`_find_newest_versioned_exe` 把 current_exe 也纳入候选，故 entry 硬链到便携 exe；删便携后硬链仍保数据，但额外复制 ver/ 副本更稳）。
- **结论**：无论杀软是否拦 spawn，都能「既显示又删除自身」。最坏情况（拦 entry 又拦 helper）= 原始 exe 在用户**下次启动**时被 .cleanup_pending 清理（延迟一启，但必删、无需管理员）。
- **git 历史核对**：f7d9105 的删除机制本就是 spawn entry + `--cleanup` + `os.remove`（无需管理员），并非我之前怀疑的「被我改写坏」；首跑 spawn 模型与两天前一致。1507 首跑不显示是**真机杀软在 bootloader 阶段杀了被 spawn 的 entry**（手动双击 entry 能跑佐证），非代码回归。
- **构建**：v2026.08.01.0019.exe（53.1MB），dist/ + dev/ver/ + build/...0019/，git 提交推送双远程。
- **验证待用户**：D:\test 删 `云集智能音乐创意台\` 目录后双击 0019 版。预期：① 启动器显示（不黑屏）；② 若 entry 未被拦 → 原始 `v...0019.exe` 立即消失；若被拦 → 下次启动经 entry/便携 exe 的 already 分支由 `.cleanup_pending` 删。看 exe 同目录 `yunji_launch_trace.log` 末尾 `spawned entry`/`self show fallback`/`scheduled self-delete helper` 确认路径。

## 0057：发现 0019 致命 bug，彻底回归 f7d9105（exe v2026.08.01.0057）
- **用户指证**：“参考 git 历史 proven 版本（新 exe 建目录+复制+建入口→从入口启动→杀旧 exe 删旧 exe+释放文件自动部署），之前为解决弹窗问题误删了指定相对路径代码”。核对 f7d9105 后确认：首跑模型本就是 `os.link(exe, entry)` + `shutil.copy2(exe, ver)` + `Popen([entry,"--cleanup="+exe])` + `os._exit(0)`，入口 already 分支**无条件** `_safe_delete(cleanup_target)`。
- **0019 致命 bug（用户“能开不删”的真凶）**：already 分支被我之前写成「仅当 `newest is None` 时才 `_safe_delete(cleanup_target)`」，正常升级/首跑（ver/ 中有新版）走另一分支**直接 return 不删** → 原始便携 exe 永不删。另首跑入口创建被 `._ensure_entry_current`（复杂版本号匹配）替代了 f7d9105 的简单 `os.link`，偏离“相对路径”原意。
- **修复（0057，回归 f7d9105 + 保留防黑屏看门狗）**：
  1. already 分支：**无条件** `if cleanup_target: _safe_delete(cleanup_target)` + `.cleanup_pending` 兜底删（保留）。
  2. 首跑入口：`entry_exe = os.path.join(deploy_dir, ENTRY_EXE_NAME)` + `os.link(exe, entry_exe)`（失败回退 copy）——恢复简单相对路径操作，删掉 `_ensure_entry_current` 调用。
  3. 首跑 spawn：保留看门狗（entry 存活 4s→`os._exit` 交出入口删自己；被杀→supervisor fallback 显示 + `.cleanup_pending` 下次删）。
  4. main.py `_deferred_init`：删除双重删除/运行中改名逻辑（之前 `_ensure_installed_exe`/`_schedule_self_delete` 触发杀软杀进程），改由 launcher 统一负责删除。
- **构建**：v2026.08.01.0057.exe（53.1MB），dist/ + dev/ver/ + build/...0057/，git 提交推送双远程。
- **教训固化**：「首跑删除自身」proven 机制已在 f7d9105 验证可用——**不要为了“防黑屏/防误删”而把简单 os.link/copy2+--cleanup+os._exit 模型复杂化**，复杂化反而破坏删除语义。看门狗/fallback 只作为防黑屏补充，且 fallback 不得触碰运行中的 exe（rename/delete 触发杀软）。

## 0416：用户再报“问题依旧”，但 0057 日志显示首跑其实已成功；对齐既定架构（exe v2026.08.01.0416）
- **关键发现（读 D:\test 日志）**：0057 在 03:56 的真机运行其实**已成功**——原始 `云集智能音乐创意台-v2026.08.01.0057.exe` 被 supervisor spawn entry（pid=48072），entry 进入 already 分支跑 main → `window shown | visible=True`，原始 exe 经 `--cleanup` 被删（磁盘全树扫描确认仅剩 `云集智能音乐创意台\云集智能音乐创意台.exe` 入口 + `ver/云集智能音乐创意台-v2026.08.01.0057.exe` 归档，原始版本号 exe 已不存在）。即“显示+删自身”两个目标 0057 均已达成，用户“问题依旧”更可能是对“入口 exe 仍留在文件夹里”的误会（那是固定名入口，非残留）。
- **用户重新描述的既定架构**：① 打包exe首跑→只建 `云集智能音乐创意台/` 文件夹+入口文件并打开入口、退出；② 入口补全文件夹、复制版本exe到 ver、启动启动器（释放文件+初始化）；③ 初始化后入口删最开始的版本exe自身。要求“别再凭空猜，按此既定设计实现”。
- **对齐实现（0416，launcher.py）**：
  - 新增 `_archive_to_ver(deploy_dir, src_exe)`：入口在删原始 exe **之前**把 `--cleanup` 指向的原始 exe 复制进 `ver/`（缺失才复制，幂等）。
  - already 分支：先 `_archive_to_ver` 再 `_safe_delete(cleanup_target)`（无条件删，回归 f7d9105 语义），并新增轨迹 `entry: archived ... -> ver/`、`entry: deleted original <path>`、`entry: pending-cleanup deleted <path>` 便于用户肉眼确认删除。
  - 首跑分支保留顺带建 `ver/` 目录并复制自身进 ver（稳健性：即便 entry 被杀，ver 归档也不缺失）；看门狗/fallback/.cleanup_pending 全保留。
- **构建**：v2026.08.01.0416.exe（53.1MB），dist/ + dev/ver/ + build/...0416/，git 提交推送双远程。
- **待用户真机验证**：全新目录（删旧 `云集智能音乐创意台\`）双击 0416 版，看 exe 同目录 `yunji_launch_trace.log` 末尾应有 `entry: archived ... -> ver/` 与 `entry: deleted original ...`，且原始版本号 exe 消失、仅留入口+ver 归档。

## 0447：修复「托盘退出后进程残留」（exe v2026.08.01.0447）
- **现象**：每次从系统托盘「退出」，仍有进程残留（主要是 powershell 服务脚本拉起的 node/python 子进程：vite 前端、ACE-Step API、gradio 等）。
- **根因**：① `_exit_all` 的进程清理是脆弱的关键字扫描——`psutil.process_iter` 仅匹配 cmdline 含 `acestep`/`qinglong`，而 ace-step-ui 目录带连字符、`acestep` 子串根本不匹配 → 前端 vite 永远漏杀；② `ServiceProcess.terminate()`（及 `_stop_service`）只 `self.process.terminate()` 杀 **powershell 父进程**，其 node/python 子进程（孙进程）不会被 SIGTERM 连带杀死 → 残留；③ `api_process.kill()` 同理只杀父。
- **修复（main.py，精确递归进程树清理）**：
  1. 新增模块级 `_kill_process_tree(proc)`：对 psutil.Process/subprocess.Popen 先优雅 `terminate` 整棵子孙树、超时 3s 再 `kill` 强杀（接受 psutil.Process 或 Popen，无 psutil 时退化为 Popen terminate/kill）。
  2. 新增 `_kill_child_processes()`：退出时遍历 `psutil.Process(os.getpid()).children(recursive=False)`，对名字∈{python,node,powershell,pwsh} 的直接子进程调用 `_kill_process_tree` 杀整棵子树。**只杀服务型进程、按进程树归属，绝误杀本启动器或升级时拉起的新版 exe**（其名非上述几类），故自更新流程不受影响。
  3. 用 `_kill_child_processes()` 替换 `_exit_all` 中原脆弱的关键字扫描块。
  4. `ServiceProcess.terminate()` 改为 `_kill_process_tree(self.process)`（连孙进程一并杀）——同时修了「停止单个服务」按钮也会残留 node/python 的隐患。
  5. `_stop_all_services` 中 `api_process.kill()` 改为 `_kill_process_tree(self.api_process)`。
- **构建**：v2026.08.01.0447.exe（53.3MB），dist/ + dev/ver/ + build/...0447/，git 提交推送双远程。
- **验证待用户**：启动服务后从托盘「退出」，应在任务管理器确认无残留 node.exe/python.exe/powershell.exe（ace-step-ui 前端、API、gradio 等）。

## 0509：修复首跑「托盘在、界面不显示」（exe v2026.08.01.0509）
- **现象**：用户反复报「第一次运行没有显示启动器界面，只有托盘图标」，跨 1507/0057/0416/0447 多版。要求参考兄弟项目 `E:\软件开发\云集智能视频创意站\dev` 及「之前能正确首跑的版本」。
- **关键证据（读 D:\test 0447 轨迹）**：entry 进程轨迹显示 `window shown | visible=True geom=(680,202,1200,988)` —— Qt 认为窗口已可见，但用户看不到。即「isVisible=True 但用户看不到」的矛盾。
- **根因（三方对齐定位）**：对比视频创意站 `_deferred_init`（proven，能正常首跑）发现两处关键差异：
  1. **视频站 resize 后显式 `self.move((screen.w-w)//2, (screen.h-h)//2)` 居中**；音乐站**只 resize 不 move**，窗口位置完全交给 OS/系统记住的布局。音乐站 config(`app/scripts/launcher_config.json`)持久化 window_size(高 988)，位置(680,202)来自 OS 窗口位置记忆——分辨率变更或小屏上会落到屏外/越界 → isVisible=True 但实际看不到。
  2. **视频站 show() 后立即 `splash.finish(self)+deleteLater()+_splash=None`**；音乐站用 `reached_full` 异步信号 + `QTimer.singleShot(800, _finish_splash_once)` 延迟收屏，期间 `WindowStaysOnTopHint` 进程内启动屏仍盖在主窗之上，且 `_splash` 从不置 None/deleteLater。
- **修复（对齐视频站 proven 实现）**：
  1. `_deferred_init` 显示前：取主屏 `availableGeometry()`，若窗口大于可用区则 resize 夹取，再 `move` 居中——强制拉回屏可见区，不依赖历史/系统位置。
  2. show() 后立即 `splash.finish(self)+deleteLater()+_splash=None`（去掉 800ms 延迟与 reached_full 重连），再 `raise_()+activateWindow()`。
  3. 新增 `_force_show_window()`：15s 兜底，若窗口仍未可见/被遮挡，强制居中+showNormal+show+raise+activate（对齐视频站 `_splash_fallback` 思路）。
- **构建**：v2026.08.01.0509.exe（53.3MB），dist/ + dev/ver/，git 提交推送双远程。
- **验证待用户**：全新目录或覆盖现有部署双击 0509 版，预期主窗口居中显示、不再「只剩托盘」。看 exe 同目录 `yunji_launch_trace.log` 末尾 `window shown` 后几何应为主屏居中坐标；若仍异常 15s 后会出现 `force_show_window: re-shown+centered`。
- **教训固化**：「窗口显示」要对齐视频站 proven 模式——**resize 后必须 move 居中 + 立即 finish/deleteLater 启动屏 + 兜底强制显示**，不要把窗口位置交给 OS 记忆（跨分辨率/小屏必踩「isVisible=True 但看不到」）。

## 0722：首跑「窗口可见但看不到」真正根因——Windows 前台锁（exe v2026.08.01.0722）
- **用户关键洞察**：「不就是正常启动 exe 吗？为啥第二次手动能正常启动、开发模式也能正常启动，第一次运行就无法正常启动」——要求研究首跑 vs 二次启动的区别。
- **三方路径对比**：首跑=versioned exe(supervisor) `Popen` 拉起 entry→supervisor `os._exit`→entry 跑 main 显示窗口（**失败**）；二次=双击 entry 由 Explorer 直接启动（**正常**）；开发=`python main.py` 由终端启动（**正常**）。entry 代码路径首跑/二次完全一致（都走 already-deployed 分支），唯一差异是 **entry 进程的启动方式**：首跑由 Popen 拉起，二次/开发由 Explorer/终端直接启动。
- **根因（Windows 前台锁）**：Popen 拉起的子进程**不继承前台权限**。entry 显示窗口时 `activateWindow()`→`SetForegroundWindow` 被 Windows 前台锁**静默拒绝** → 窗口 `isVisible()=True`（Qt 已 show）但停留在后台、用户看不到（只剩托盘）。Explorer/终端启动的进程自带前台权，故二次/开发正常。0509 轨迹铁证：`window shown | visible=True geom=(680,232,1200,988)` 且 15s 后 `force_show_window` 仍 visible=True，但用户就是看不到——典型「可见但非前台」。
- **修复（双保险）**：①launcher.py：supervisor `Popen([entry,...])` 后立即 `ctypes.windll.user32.AllowSetForegroundWindow(child.pid)`（Win32「父进程授予子进程前台权」标准机制）。②main.py：新增 `_force_foreground()` 用 `AttachThreadInput(绑当前前台线程输入队列)+SetForegroundWindow+BringWindowToTop+ShowWindow(SW_SHOW)` 突破前台锁（Win32 公认「子进程抢前台」做法），在 `_deferred_init` 显示后立即+500ms 双调用，`_force_show_window` 兜底也调用。
- **构建**：v2026.08.01.0722.exe（53.3MB），dist/ + dev/ver/，git 提交推送双远程。
- **验证待用户**：全新目录双击 0722 版便携 exe，预期首跑主窗口立即置顶显示（不再「只剩托盘」）。这是首次针对「首跑 vs 二次」差异的根因修复，非再凭猜测。
- **教训固化（写入 MEMORY.md）**：PyInstaller onefile「supervisor Popen 拉起 entry」模型下，entry 是 Popen 子进程、不继承前台权限，其窗口 activateWindow 会被 Windows 前台锁拒绝 → 必须 supervisor 侧 `AllowSetForegroundWindow(child_pid)` + entry 侧 `AttachThreadInput+SetForegroundWindow` 突破前台锁。这是「Popen 拉起的 GUI 子进程」通病，与「Explorer/终端直接启动」行为不同的根因。
