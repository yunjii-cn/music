# 2026-07-18 工作日志

## 音乐工作台前端接入官网登录页门控
- 目标：启动器打开的「音乐工作台」前端（`dev/app/ace-step-ui`，React+Vite，青龙前端端口 3070/Vite 开发服，3060/Express 服务 dist/）加登录门控，改用官网统一登录页 `https://music.yunjii.cn/login?embed=1&redirect=<本应用 origin>&state=<随机>`。
- 重写 `dev/app/ace-step-ui/LoginGate.tsx`：未登录 → 整页 302 跳官网登录页；回跳 URL 带 `yunji_user`（base64 JSON，含 social_uid/openid、nickname、faceimg、token）→ 解码 → `loginWithUM` → 后端 `/api/auth/um` 换本地 JWT → 解锁。state + visited 标记防 CSRF 与重定向死循环。
- `UM_LOGIN_URL` 默认改为 `https://music.yunjii.cn/login`（原 `/um/index.php` 旧 iframe 方案已废弃）；可用 `VITE_UM_LOGIN_URL` 覆盖，已补进 `.env.example`。
- 已重新 `vite build`，dist/ 已含新门控（验证 bundle 含 yunji_user / music.yunjii.cn/login / embed）。
- 注意：官网登录页需把本应用 origin（如 http://localhost:3070/）加入 redirect 白名单，否则回跳被拒（用户侧配置，本次未动）。
