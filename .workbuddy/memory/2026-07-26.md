# 2026-07-26 工作日志

## P0 落地：训练数据一键准备（scan→转写→打标→预处理 全链路编排）
- 用户拍板实施 P0；令牌轮换用户明确表示**不用管**（开源项目、无安全顾虑），**以后不再提醒**。
- **架构**：完全复用 Python 侧已有端点（/v1/dataset/scan、auto_label_async(transcribe_lyrics=true 含 ACE-Step/acestep-transcriber 歌词转写 + LLM caption/BPM/Key)、save、preprocess_async），Node 侧新增编排服务，零 Python 改动。仿 quickTrain.ts 的内存任务状态机+前端轮询模式。
- **新增/修改文件**：
  - `dev/app/ace-step-ui/server/src/services/dataPrep.ts`（新增）：startDataPrep/getDataPrepTask/cancelDataPrep；流程 env-profile 取 lora_outputs_root → scan → auto_label_async(save_path=<root>/<name>/dataset.json 边打标边持久化) → save → preprocess_async(输出 <root>/<name>/tensors)；进度分配 scan 0-10%、label 12-70%、save 72%、preprocess 75-99%；支持取消（cancelled 标志 + assertAlive）。
  - `server/src/routes/training.ts`：+3 路由 POST /data-prep、GET /data-prep-status/:id、POST /data-prep-cancel/:id。
  - `services/api.ts`：trainingApi.dataPrep/getDataPrepStatus/cancelDataPrep。
  - `components/TrainingPanel.tsx`：dataset Tab 顶部新增紫色"训练数据一键准备"卡片（文件夹/数据集名/激活标签/含人声勾选+进度条+取消）；完成后自动回填 loadJsonPath/savePath/preprocessOutputDir/trainingTensorDir 并刷新样本表，用户可直接切训练页开训。
  - `i18n/translations.ts`：新增 9 个键 ×4 语言（en/zh/ja/ko），锚点在各语言 `scanning:` 行前。
- **验证**：server `npx tsc --noEmit` 通过；`npx vite build` 成功（dist/assets/index-BWBj_i_d.js 含"训练数据一键准备"字样）。
- **分发**：exe 不含前端（运行时走松文件 app/ace-step-ui/），无需重建 exe。打了补丁包 `dev/dist/训练数据一键准备-patch-v2026.07.26.zip`（9 文件：5 源码 + dist/ 全量，zip 内路径以 app/ 开头，解压到部署根目录如 D:\test\云集智能音乐创意台\ 覆盖即可）。server 生产跑 `tsx watch src/index.ts` 直接吃源码，无需编译。
- 待用户验证：真实音频文件夹跑一遍一键准备（转写模型 acestep-transcriber 首次会从 HF 下载）。

## Git 推送补账 + P1 落地：XL(4B)底座 LoRA 训练变体注册
- 用户指出"我说继续是指继续 P1，你只推送没继续"——此前把「继续」误解为只处理 git。教训：**用户说"继续"优先指上一轮明确预告的下一步任务**。
- **Git**：提交 bcea938（P0+flash_attn修复+venv兜底，12 文件）与 f61562b（P1，16 文件），连同此前 8 个本地提交全部推上 origin(GitHub yunjii-cn/music)，现与远程完全同步。
- **P1 实施（按 docs/xl-training-support-plan.md §3.1-3.5，全加法）**：
  - `training_v2/model_loader.py:_VARIANT_DIR` + `training_v2/cli/args.py:VARIANT_DIR_MAP`（**方案漏列的第二份映射表**，被 config_builder/validation/common 引用）注册 xl→acestep-v15-xl-turbo / xl-sft / xl-base。
  - `model_discovery.py`：detect_base_model 在 is_turbo 判断**之前**优先匹配 xl 家族（修 xl-turbo 误判 turbo），精确区分 xl/xl-sft/xl-base；_BASE_DEFAULTS 补 XL 三项。
  - `quick_presets.py`：VARIANT_DEFAULTS/ALL_VARIANTS 补 XL；_VARIANT_PREFERENCE=[xl-sft,sft,xl-base,base,xl,turbo]；**修存量 bug：resolve_training_params 引用未定义 tier（NameError），补 tier="full" 参数**（Python 侧无调用方所以一直没炸，Node 的 quickTrain.ts 镜像了该逻辑）。
  - `api/train_api_service.py`：env-profile 的 downloaded 白名单 ("turbo","base","sft") 改用 ALL_VARIANTS（否则 XL 下载了也不出现在下拉）；missing_variants 仍只算 1.5B（XL 是可选件，不触发缺失提示）。
  - `cli/train_vanilla.py:96`：vanilla 非 turbo 警告分支补 xl-base/xl-sft。
  - 前端：TrainingPanel 底座选择器**是写死的**（['turbo','sft','base']，非 API 动态渲染）——直接加第 4 个 XL 按钮 + 选中时琥珀色警示条（18.8GB/显存≥16GB/1.5B LoRA 不可复用）；shiftDefaults 补 xl 家族；i18n 新键 xlVariantHint/xlVariantWarning ×4 语言；两份 qualityPresets.ts 镜像同步。
  - 新增 `dev/app/verify_xl_training.py`：§4.2 冒烟校验脚本（[0]权重目录→[1]加载→[2]目标层→[3]LoRA注入→[4]可选前向），部署机用 scripts\.venv\Scripts\python.exe 跑。
- **gitignore 大坑**：`.gitignore:38` 无锚点 `data/` 规则误伤 `ace-step-ui/data/` 与 `server/src/data/` 前端源码目录——qualityPresets.ts/genres.ts 等**从未进过版本控制**。已加 `!dev/app/ace-step-ui/data/` 和 `!dev/app/ace-step-ui/server/src/data/` 白名单并首次入库。
- **验证**：py_compile 全过；AST+import 断言测试全绿（变体映射一致性/误判修复/偏好序/回归）；server tsc 通过；vite build 成功且 dist 含 XL 代码。
- **分发**：`dev/dist/XL底座LoRA训练-patch-v2026.07.26.zip`（15 文件=6 Python+4 前端源码+verify 脚本+dist 全量，解压部署根目录覆盖）。**注意 Python 改动在 app/acestep/ 松文件，不需要重建 exe**。
- **待办（宣布"支持XL"前必须）**：用户在训练机「模型管理」下载 acestep-v15-xl-turbo（18.8GB）→ 跑 verify_xl_training.py → [1][2][3] 全绿 + 实训 2 step loss 有限下降。

## 🚨 严重回归修复：install-env.ps1 语法错误导致部署全挂（v2026.07.26.0144）
- 用户实测 v2332 exe 部署直接报 `The Try statement is missing its Catch or Finally block`（行380 ParserError）——**PowerShell 解析期报错，整脚本一行未执行即返回 1**，比原 flash_attn 失败更糟（部署完全不可用）。
- **根因**：bcea938 那轮我给 flash_attn 段加 try/catch 时，`if { try { ... } }` 的收尾大括号顺序颠倒（`}` `} catch` 应为 `} catch` `}`），**改完没做 ps1 语法校验就打包了**。
- **修复**：括号配对修正（`catch` 移入 `if` 块内闭合）；`[System.Management.Automation.Language.Parser]::ParseFile` 校验源文件+全部 10 个打包 .ps1+从 exe 提取的那份，全部 NO-PARSE-ERRORS。
- **防复发闸门（重要惯例）**：`build-version.py` build_exe() 在 `--add-data` ps1 前新增**强制语法校验**——powershell Parser.ParseFile 扫全部 .ps1，任何 ParserError 直接 sys.exit(1) 拒绝构建。已实测生效（"✓ 10 个 .ps1 语法校验通过"）。**以后任何 .ps1 改动，构建即自动校验；手改后也应先跑 PowerShell Parser 校验再交付。**
- 交付 `v2026.07.26.0144.exe`（36.9MB，build/+dist/）；提交 1358792 已推 GitHub。

## 部署索引根治：移除全局 UV_EXTRA_INDEX_URL + 失败不假跳模型管理（v2026.07.26.0225）
- 用户 01:58 日志：venv 种子包/wheel-stub 装不上，报 `Failed to fetch https://download.pytorch.org/whl/cu128/pip/ ... tls handshake eof`；且部署失败后仍跳到模型管理页（用户以为"还没部署完就跳"）。
- **git 回溯结论**：步骤1-4（venv/torch/依赖）代码在 66be154（只差 flash_attn 时代）与 HEAD 间**一行未变**，我的改动全在 flash_attn 段——这次不是我改坏的，是**从一开始就存在的设计缺陷被网络波动引爆**：全局 `$Env:UV_EXTRA_INDEX_URL=download.pytorch.org/whl/cu128` 让 uv 装 pip/wheel-stub 等纯 PyPI 包也先查 PyTorch 官源；官源国内时通时断，一断整个部署第一步就挂。昨天 06:23 成功纯属当时官源可达。
- **修复（提交 2310fb2，exe v2026.07.26.0225）**：
  - `install-env.ps1`：删全局 UV_EXTRA_INDEX_URL；torch 安装改 **阿里云 `mirrors.aliyun.com/pytorch-wheels/cu128/` 镜像优先（--find-links，已验证含 torch/torchaudio 2.9.0+cu128 cp312 win_amd64）**，官方源仅在该条命令内 `--extra-index-url` 兜底；`uv venv --seed` 失败即 Check 报错退出（原静默继续）。
  - `main.py _enable_buttons`：`deploy_ok = getattr(self,'_env_installed_in_last_deploy',False)` 门控——失败**不再跳模型管理页**（留在部署页看日志）、进度条定格"❌ 部署未完成"（原恒显"✅ 完成"）；超时文案 30→90 分钟。
- 校验：CArchive 提取 exe 内 ps1+main.py 七项检查全 OK、pyi_splash=0、残留 UV_EXTRA_INDEX_URL 赋值行=0。
- **教训/惯例**：任何"全局包索引环境变量"都是单点故障；第三方源只应限定在需要它的那一条安装命令内。

## 模型下载 ModuleNotFoundError + flash_attn 仍失败 根治（v2026.07.26.0420）
- 用户 03:16 日志：环境部署**这次成功**（阿里云 torch 满速、依赖全装、exit 0），但暴露两个旧 bug：
  ① 模型下载 `python -m acestep.model_downloader` → `ModuleNotFoundError: No module named 'acestep'`；② flash_attn 码云分卷失败被跳过后仍装不上。
- **根因①（acestep 缺失）**：用户部署目录 `D:\test\...\app\` 是**不完整副本**——缺 `app/acestep\` 子目录（只有 main.py/scripts 等）。命令逻辑本身正确（沙箱实测 `PYTHONPATH=app`+`cd app`+`python -m acestep.model_downloader` 在完整发布包里能找到 acestep）；`acestep` 在 git 正常跟踪、非 ignore，是发布包解压/复制不完整所致。**关键认知：单文件 exe 必须配「完整发布文件夹」(build/ 那份含 app/acestep) 才能跑，裸 exe 单独拿出来会因缺 acestep 等松文件而崩。**
- **根因②（flash_attn 假跳过）**：旧 `Download-GiteeMultipart` 若 curl 抛异常，异常外溢到外层 try/catch 被吞 → GitHub 镜像兜底（步骤②/③）整段不执行 → 永远装不上。Gitee 源本身可达（沙箱 200 + 90MiB），用户机器失败是网络屏蔽/抖动。
- **修复（提交 564ea08，exe v2026.07.26.0420）**：
  - `main.py`：新增 `resolve_acestep_syspath(start_dir)`——向上遍历目录树+兜底 glob 找含 `acestep/__init__.py` 或 `acestep/model_downloader.py` 的目录，返回其**父目录**；下载线程/删除线程的 PYTHONPATH 注入与 4 处进程内 `sys.path.insert` 全部改用它（不再假设 acestep 必在 base_dir 直接子级）；加 preflight——找不到 acestep 时给**明确可操作报错**而非裸 ModuleNotFoundError。
  - `install-env.ps1`：重写 flash_attn 段，新增**永不抛异常**的 `Invoke-FaDownload` 包裹每个源；Gitee 分卷失败(异常/过小)→ `$downloaded=$false` → **必走 GitHub 镜像竞速(ghproxy.net/mirror.ghproxy.com)兜底 → 官方源直连兜底**；全程受 `$faBudgetSec=600` 约束不会挂起；失败只警告跳过不影响部署。
- **交付**：完整发布文件夹 `build/云集智能音乐创意台-v2026.07.26.0420/`（含 app/acestep 390 文件 + 新 install-env.ps1 + 新 exe）；并补部署 exe 到 dev/dist/dev/ver/dist 三处（构建驱动 _deploy_to_dev 默认只落 PROJECT_ROOT/dist，需手动补 dev/dist/dev/ver）；增量补丁包 `dev/dist/修复-acestep缺失与flash_attn兜底-patch-v2026.07.26.0420.zip`（40.5MB=缺失 app/acestep 9.4MB + 新 install-env.ps1 + 新 exe + 对齐 main.py，**解压到现有部署根目录即可、保留已装 venv**）。
- **验证**：PowerShell Parser 校验 install-env.ps1 通过；py_compile main.py 通过；沙箱 find_spec 确认发布包内 acestep 包与 model_downloader 子模块均可定位；Gitee 分卷1 HEAD 200+94371840 字节确认源可达。GitHub 镜像因沙箱出网 TLS 限制无法实测，但代码兜底链已就位。
- **已推 GitHub**：564ea08 → origin/main（push 成功）。

## 部署后19分钟冻结根治（v2026.07.26.0628）
- 用户 05:38 日志暴露：部署完成(05:46:10)→自动下载模型(06:05:24)中间**冻结 19 分钟**，UI 完全无响应。
- **根因（双重灾难）**：① `resolve_acestep_syspath()` 旧版兜底 `glob.glob(os.path.join(root,"**","acestep","model_downloader.py"),recursive=True)` 中 `roots` 含盘根 `D:\` → **从 D:\ 递归扫全盘**，每次数分钟；② `_load_model_list()` → `_check_main_model_exists()`/`_check_model_exists()`/`_get_model_verify_info()`/`_get_model_size_bytes()` 四个函数全在**主线程**调 `resolve_acestep_syspath`+`from acestep.model_downloader import`（即便 acestep 存在也冷加载 torch 30-60s）；10+ 模型 × 全盘扫描 = 19 分钟冻结。
- **修复（提交 f576c82，exe v2026.07.26.0628）**：
  1. `resolve_acestep_syspath`：**删除递归 glob 兜底**，只做 O(1) 直接祖先目录检查。
  2. 新增模块级纯文件系统辅助：`_FS_MAIN_MODEL_COMPONENTS`/`_FS_MODEL_VALIDATION_INFO` 常量 + `_fs_get_checkpoints_dir`/`_fs_get_directory_size`/`_fs_required_file_present`/`_fs_check_main_model_exists`/`_fs_check_model_exists` 函数，精确复刻 model_downloader.py 逻辑但**零 import、零 glob 扫盘**。
  3. 重写 4 个主线程方法（`_check_main_model_exists`/`_check_model_exists`/`_get_model_verify_info`/`_get_model_size_bytes`）+ `_load_model_list` 的 checkpoints_dir 获取——全部改用纯 FS 辅助，不再 `from acestep.model_downloader import`。
  4. worker 线程（ModelDownloadThread/DeleteThread/VerifyThread）的 import 保留（不冻结 GUI）。
- **验证**：用户 07:24 日志确认冻结消失——部署完成(07:37:04)→下载尝试(07:37:05)仅 **1 秒**（之前 19 分钟）。
- **遗留**：用户 `D:\test\...\app\` 仍缺 `acestep\`（未应用补丁包），模型下载 preflight 正确报错。需用户应用 `dev/dist/修复-部署后冻结19分钟-acestep缺失-patch-v2026.07.26.0628.zip`（40.5MB，含 390 个 acestep 文件+新 exe+install-env.ps1，解压到部署根目录覆盖即可）。
- **教训**：① 主线程绝不递归 glob 扫盘（从盘根扫全盘=数分钟~数十分钟冻结）；② 主线程绝不 import 重型包（torch 冷加载 30-60s 冻结）；③ 需要查文件就纯 os.path，需要重型包就丢 worker 线程。

## 单 exe 自包含必要目录（payload.zip）落地（v2026.07.26.0901）
- **用户明确要求（拍板架构）**："单exe就应该包含必要的文件目录，来保证部署维护的完整性。一键部署维护无法覆盖的目录文件应该打包在exe中。" → 否定"裸 exe 必须配完整发布文件夹"的旧认知（见上 0420 段），改为**单 exe 自包含所有必要目录**。
- **方案（提交待做，exe 已构建 v2026.07.26.0901）**：
  - `build-version.py`：在 ps1 `--add-data` 循环之后、`pyinstaller_args.append("launcher.py")` 之前，构建期用 `zipfile` 打 `payload.zip`（含 `acestep/` 全部 + `ace-step-ui/` 精简源码），排除 `__pycache__/node_modules/docs/.git/.vscode` 及 `ace-step-ui/server/{public,data,node_modules}`（运行时音频/SQLite 数据）；然后 `--add-data payload.zip;.` 打进 exe；构建后清理磁盘松文件（沙箱回收站不可用导致清理失败，但 exe 已含 payload 不影响）。
  - `launcher.py`：新增 `_extract_payload()`（sys.frozen 时，若 `app/acestep/__init__.py` 缺失则从 `sys._MEIPASS/payload.zip` 解压到 `app/`；已存在则跳过幂等）；在 `_run_supervisor()` 的 `_self_relocate()` 之后、`import main` 之前调用（supervisor 路径，行418；splash-child 路径不调避免误杀）。
- **验证（全绿）**：
  1. exe 体积 37→**52.7MB**（payload.zip 15.9MB 已打进）；
  2. 原始字节 `b'payload.zip'` 在内、`b'pyi_splash'` 不在（=0，不崩）、`b'main.py'` 在内（哨兵逻辑走打包副本）、`b'_PYI_SPLASH_IPC'`=1（引导器惰性常量，无害）；
  3. **功能测试**：模拟 `sys.frozen=True`+`sys._MEIPASS`(含 payload.zip)+`YUNJI_INSTALL_ROOT`，import launcher 模块调 `_extract_payload()` → 实测解出 `app/acestep/__init__.py`+`model_downloader.py`+`app/ace-step-ui/`，幂等 OK；
  4. payload.zip 抽样：acestep/ 390 条目（含 `__init__.py`/`model_downloader.py`）、ace-step-ui/ 245 条目（无 node_modules、无 server/public、无 server/data）。
- **承诺**：裸 exe 单独拿出去也能跑（缺 acestep 时首次运行自动从 exe 内解压）。完整发布文件夹仍照常构建（build/ 那份），但不再是运行必需。
- **待办**：提交 git 并推送；在部署机实测"裸 exe 复制到空目录双击"能自解压 acestep 并正常启动。
